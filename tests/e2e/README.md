# End-to-end tests

Playwright journeys live in this directory and are grouped into `e2e`, `a11y`, and `visual`
projects. The root scripts are the canonical interface. The required launch journeys in
`AGENTS.md` section 14 must be added as their vertical slices are implemented.
